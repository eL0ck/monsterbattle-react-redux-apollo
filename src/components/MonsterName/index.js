import React from 'react';

import './styles.css';

function MonsterName ({name}) {
  return <div className={'MonsterName'}>{name}</div>
}
//const MonsterName = ({name}) =>
	//<div className={'MonsterName'}>{name}</div>

export default MonsterName
